syntax = "proto3";
package tendermint.mempool;

option go_package = "github.com/lazyledger/lazyledger-core/proto/tendermint/mempool";

import "tendermint/types/types.proto";

message Txs {
  repeated tendermint.types.Tx txs = 1;
}

message Message {
  oneof sum {
    Txs txs = 1;
  }
}
